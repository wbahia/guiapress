<%- include("partials/header.ejs")%>  
<%- include("partials/homenavbar.ejs")%>  

<body>
    <div class="container">
        <hr>
        <div class="jumbotron">
            <h1 class="display-4">Bem vindo!</h1>
            <p class="lead">Este é um simples blog testando o desenvolvimento com Node.js.</p>
            <hr class="my-4">
        </div>
        
        <% articles.rows.forEach(article => {  %>
            <div class="card-header">
                <h2><%= article.title %></h2>
            </div><br>
            <div class="card-body">
                <a href="/<%= article.slug %>" class="btn btn-primary">Ler Artigo</a>
            </div><br>
        <% }) %>
        
        <hr>
        <div class="row">
            <div class="col offset-md-10">
                <% if(next) {%>
                    <a href="/page/2" class="btn btn-primary">Next >>></a>
                <% } %>
            </div>
        </div>
        <br>
    </div>
</body>

<%- include("partials/footer.ejs")%>
